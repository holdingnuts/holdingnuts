#ifndef _PLATFORM_H
#define _PLATFORM_H

/*
#if (PLATFORM_STR == win32)
# define PLATFORM_WINDOWS
#elif (PLATFORM_STR == macosx)
# define PLATFORM_MACOSX
#elif (PLATFORM_STR == unix)
# define PLATFORM_UNIX
#else
# define PLATFORM_UNKNOWN
# warning "Configured with unknown platform"
#endif
*/

#endif /* _PLATFORM_H */
